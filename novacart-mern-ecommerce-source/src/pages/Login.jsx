import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useStore } from '../context/StoreContext';
export default function Login(){const [mode,setMode]=useState('login'),[form,setForm]=useState({name:'',email:'student@example.com',password:'password'}),[error,setError]=useState('');const {setUser}=useStore();const nav=useNavigate();async function submit(e){e.preventDefault();setError('');try{const r=await fetch(`/projects/novacart/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw new Error(d.message);setUser(d.user);localStorage.setItem('novacart-token',d.token);nav('/');}catch(err){setError(err.message||'Unable to authenticate');}}return <main className="auth"><form onSubmit={submit}><div className="kicker">ACCOUNT</div><h1>{mode==='login'?'Welcome back':'Create account'}</h1>{mode==='register'&&<input placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/>}<input type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/>{error&&<p className="error">{error}</p>}<button className="primary full">{mode==='login'?'Sign in':'Register'}</button><button type="button" className="switch" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Need an account? Register':'Already registered? Sign in'}</button><small>Demo mode works without MongoDB. Add MONGODB_URI for persistent accounts.</small></form></main>}
