import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useStore } from '../context/StoreContext';
export default function Checkout(){const {cart,total,user,setCart}=useStore();const nav=useNavigate();const [form,setForm]=useState({name:user?.name||'',email:user?.email||'',address:'',city:'',country:'Algeria'});async function submit(e){e.preventDefault();const r=await fetch('/projects/novacart/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.email,items:cart,total,shipping:form})});const d=await r.json();if(r.ok){setCart([]);nav('/success',{state:{order:d}})}}return <main className="shell checkout"><form onSubmit={submit}><div className="kicker">SECURE CHECKOUT</div><h1>Shipping details</h1><div className="form-grid"><input required placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="wide" required placeholder="Address" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/><input required placeholder="City" value={form.city} onChange={e=>setForm({...form,city:e.target.value})}/><input required placeholder="Country" value={form.country} onChange={e=>setForm({...form,country:e.target.value})}/></div><div className="payment-note">Demo checkout: no real payment is collected. The order API validates and confirms the order.</div><button className="primary">Place order · ${total.toFixed(2)}</button></form></main>}
